% Iteration Tree
% Author: Stefan Mesken
\documentclass{standalone}

\usepackage{tikz}
\usetikzlibrary{calc}
%\usetikzlibrary{snakes}
%\usetikzlibrary{decorations.pathmorphing}

\begin{document}
\begin{tikzpicture}
  \coordinate (root) at (0,0);
  \coordinate (a) at (1,4);
  \coordinate (b) at (-1,3);
  \coordinate (c) at (-1,4.5);

  \coordinate (2nda) at (8,4);
  \coordinate (2ndb) at (6,3);
  \coordinate (2ndd) at (6,4.5);
  \coordinate (2nde) at (6,6);

  \coordinate (2ndroot) at (7,0);
  
  
  % left tree
  \draw (root) -- ($(root) + (-3,5)$);
  \draw (root) -- ($(root) + (3,5)$);

  
  % models
  \fill (a) circle[radius=1pt] node[right]
  {$\mathcal{M}^{\mathcal{T}}_{\alpha}$};
  \fill (b) circle[radius=1pt]
  node[right]
  {$\mathcal{M}^{\mathcal{T}}_{\beta}$};
  \fill (c) circle[radius=1pt]
  node[right]
  {$\mathcal{M}^{\mathcal{T}}_{\beta+1}$};

  % extenders
  \draw (b) -- node[right] {$E^{\mathcal{T}}_{\beta}$} (c);

  % right tree
  \draw (2ndroot) --($(2ndroot) + (4,7)$);
  \draw (2ndroot) --($(2ndroot) + (-4,7)$);

  % models
  \fill (2ndb) circle[radius=1pt] node[right] {$\mathcal{M}^{\mathcal{W}}_{\beta}$};
  \fill (2nda) circle[radius=1pt] node[right] {$\mathcal{M}^{\mathcal{W}}_{\alpha}$};
  \fill (2ndd) circle[radius=1pt] node[right] {$\mathcal{M}^{\mathcal{W}}_{\alpha+1}$};
  \fill (2nde) circle[radius=1pt] node[right] {$\mathcal{M}^{\mathcal{W}}_{\alpha+2}$};

  % extenders
  \draw (2ndb) -- node[right] {$F$} (2ndd);
  \draw (2ndd) --node[right] {$p(E^{\mathcal{T}}_{\beta})$} (2nde);

  % embeddings
  \draw[->] (b) -- node[above right]{$\pi_{\beta}$} (2ndd);
  \draw[->] (c) -- node[above right]{$\pi_{\beta+1}$} (2nde);
\end{tikzpicture}

\end{document}
